Work through pseudocode.

Initialise s
Choose a from s using e-greedy
Repeat for each step of episode:
		Take action a, observe r, s'
		Choose a' from s' using e-greedy and w_old
		Q(s, a, w_new) = Q(s, a, w) + alpha(r + gamma * Q(s', a', w_old) - Q(s, a, w))
		s = s'
		a = a'

Generate data through table-SARSA.


Shuffle data and try to get a network working.

